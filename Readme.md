# **Student Record System (CRUD) – C Project**

این پروژه یک **سیستم مدیریت دانشجو** هست که با زبان **C** نوشته شده.
این اولین پروژه من با زبان C هست و برای تمرین مفاهیم پایه‌ای این زبان طراحی شده. ✨

---

## **📌 امکانات پروژه**

این سیستم شامل عملیات پایه‌ای CRUD هست:

- **Add Student** – اضافه کردن دانشجو
- **Update Student** – ویرایش اطلاعات دانشجو
- **Delete Student** – حذف دانشجو
- **Display All Records** – نمایش تمام رکوردها
- **Save Records to File** – ذخیره‌سازی در فایل
- **Auto ID Handling** – تولید خودکار ID برای هر دانشجو

---

## **📂 ساختار داده**

هر دانشجو به شکل زیر ذخیره می‌شود:

```c
typedef struct {
    int id;
    char name[50];
    double gpa;
} Student;
```

---

## **💾 ذخیره‌سازی دیتا**

اطلاعات دانشجوها داخل فایل `students.db` ذخیره می‌شن و هر بار اجرای برنامه دانلود می‌شن.

فرمت ذخیره‌سازی:

```
id,name,gpa
```

---

## **🚀 نحوه اجرا**

```bash
gcc main.c -o students
./students
```

---

## **🎯 هدف پروژه**

این پروژه برای یادگیری و تمرین:

- مدیریت آرایه‌ها
- مدیریت فایل‌ها (File I/O)
- ساخت منو
- کار با ساختارها (Struct)
- پیاده‌سازی CRUD در C

طراحی شده و پایه خیلی خوبی برای پروژه‌های بعدی محسوب می‌شه.

---
